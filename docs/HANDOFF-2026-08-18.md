# Handoff — 18 أغسطس 2026

**الفرع:** `main`  
**المستودع:** https://github.com/ainoamn/WAZEN  
**الإنتاج المخصص:** https://wazen.bhd-om.com  
**نشر Vercel:** https://wazen-roan.vercel.app  

**التقرير المفصّل للمنصة والتقنيات والمجموعة:** [PLATFORM-REPORT-2026-08-18.md](./PLATFORM-REPORT-2026-08-18.md)

المرجع السابق: [HANDOFF-2026-08-17.md](./HANDOFF-2026-08-17.md).

على الجهاز الآخر: `git pull origin main` ثم `npm install` إن لزم. لا ترفع ملفات `public/brand/*-with-bg.png`.

## ماذا أُنجز وُثّق ودُمج

كل فروع العمل حتى هذا التسليم على `main` بعد دمج [#53](https://github.com/ainoamn/WAZEN/pull/53).

| المجال | الطلبات |
|--------|---------|
| باقات، حصص، إدارة، تعريب | [#10](https://github.com/ainoamn/WAZEN/pull/10)–[#16](https://github.com/ainoamn/WAZEN/pull/16) |
| جلسة، طباعة، سكون، رئيسية، بناء | [#17](https://github.com/ainoamn/WAZEN/pull/17)–[#29](https://github.com/ainoamn/WAZEN/pull/29) |
| متصفح واحد، منع تجاوز `/login` | [#30](https://github.com/ainoamn/WAZEN/pull/30)–[#32](https://github.com/ainoamn/WAZEN/pull/32) |
| تسجيل جوجل وإرجاع النطاق المخصص | [#33](https://github.com/ainoamn/WAZEN/pull/33)–[#42](https://github.com/ainoamn/WAZEN/pull/42) |
| تقرير المنصة وهذا التسليم | [#43](https://github.com/ainoamn/WAZEN/pull/43) |
| فلاش خطأ الرئيسية قبل تسجيل الدخول | [#47](https://github.com/ainoamn/WAZEN/pull/47) |
| تنقّل فوري بدون شاشة الشعار | [#48](https://github.com/ainoamn/WAZEN/pull/48) |
| تغيير كلمة المرور من الإعدادات | [#50](https://github.com/ainoamn/WAZEN/pull/50) |
| إصلاح تعليق التحميل بعد الشعار | [#51](https://github.com/ainoamn/WAZEN/pull/51) |
| تسجيل موحّد BHD Identity | [#53](https://github.com/ainoamn/WAZEN/pull/53) |

## `/home` و`/dashboard` تتوقفان على الشعار

بعد [#51](https://github.com/ainoamn/WAZEN/pull/51) و[#53](https://github.com/ainoamn/WAZEN/pull/53) الصفحة المسجّلة بقيت على شعار وازن والقلب و«جاري التحميل…» ولا تنتقل للواجهة.

سببان متراكبان في `GET /api/dashboard` (وأي مسار يستدعي `ensureSchema` مثل `/api/auth` لبوابة الإدارة):

1. **`initializeSchema` عند المخطط الحالي.** كان يُفترض أن `schema_meta.version >= SCHEMA_VERSION` يعيد فوراً. مسار جوجل ثم SSO أضاف على نفس المسار `CREATE TABLE oauth_identities` و`ensureBhdSubColumn` (PRAGMA + ALTER + فهرس فريد). على Neon هذه جولات إضافية أو قفل فهرس في كل عزل بارد. إن تأخّر تحديث `schema_meta` يُعاد المسار الكامل للترقيع (عشرات الثواني) قبل كتابة الإصدار، فيقطع Vercel الطلب ويُعاد الترقية في الطلب التالي.
2. **`reconcileMemberLedgers` في كل GET.** تحديث مرتبط لكل الأعضاء عبر تجميعات `transactions`. `GET` يمرّر `refreshDerived: false` لتخطي `generatePersonalOccurrences` لكن إعادة بناء الدفاتر لم تكن مربوطة بهذا العلم.

العميل يبقي الشعار طالما `loading && !data`. مهلة `AbortController` وحدها لا تكفي إن بقي `fetch` معلّقاً دون رفض، فيبقى `finally` بلا تنفيذ.

الإصلاح (بدون لمس أرصدة المحافظ أو بيانات الهوية):

- مسار المخطط الحالي: `markSchemaReady()` ثم عودة. إنشاء OAuth و`bhd_sub` يبقى في `ensureSchemaPatches` للترقية فقط. `SCHEMA_VERSION` يبقى **13**.
- `loadDashboard`: إعادة البناء و`generatePersonalOccurrences` فقط عندما `refreshDerived !== false`. `GET` وإعادة تشغيل Idempotency تبقى `false`. بعد POST ناجح: `refreshDerived: true` حتى تُحدَّث الدفاتر بعد الكتابة.
- العميل: `Promise.race` مع المهلة حتى يظهر «تعذر التحميل» وزر إعادة المحاولة بدل الشعار إلى ما لا نهاية. مهلة اللوحة **12 ث**، وحدة الإدارة **15 ث**، بوابة `/admin` **20 ث**.

لا تُشارك `DATABASE_URL` مع هوية BHD. SSO يبقى متوقفاً حتى ضبط الأسرار.

## حساب BHD الموحّد

المواصفة المعتمدة: [BHD-IDENTITY-SSO.md](./BHD-IDENTITY-SSO.md) من [ainoamn/ONE-BHD](https://github.com/ainoamn/ONE-BHD).

وازن أصبح عميل OIDC `bhd-wazen`: `/api/auth/bhd/start` و`/callback`. يُربط المستخدم المحلي بـ `users.bhd_sub` دون نسخ المحافظ أو كلمات المرور أو كوكي الهوية. إن وُجد `BHD_OAUTH_CLIENT_SECRET` تتحوّل `/login` إلى الهوية؛ وإلا يبقى البريد وجوجل.

أدوار الإدارة لا تُمنح من التوكن. الخروج يمسح جلسة وازن ثم `/oauth/end-session`.

**مدمج في `main`:** [#53](https://github.com/ainoamn/WAZEN/pull/53). بعد ضبط أسرار الهوية على Vercel يتحوّل `/login` إلى `id.bhd-om.com`. بدون السر يبقى الدخول المحلي.

## تعليق الرئيسية والإدارة بعد إزالة الشعار

[#48](https://github.com/ainoamn/WAZEN/pull/48) أخّر الصفحة لأنها كانت تطلب الفوترة والمستندات والإدارة **مع** `/api/dashboard`. الصفحة تبقى على «جاري التحميل…» / «جاري التحقق…».

الإصلاح: شعار وازن + القلب في التحميل الأول. طلبات التحميل المسبق بعد نجاح الصفحة. مهلة 12 ثانية ثم إعادة المحاولة (كانت 25 ث ثم قصّرت لأن الطلب نفسه كان ثقيلاً).

**مدمج في `main`:** [#51](https://github.com/ainoamn/WAZEN/pull/51). بعد نشر Vercel: أول فتح لـ `/home` أو `/admin` يظهر الشعار والقلب ثم الصفحة. إن تجاوز الطلب المهلة يظهر زر إعادة المحاولة. التنقّل داخل الحساب يبقى فورياً من الذاكرة.

## تغيير كلمة المرور في صفحة المستخدم

الإعدادات كانت تعرض الاسم والبريد فقط. تغيير كلمة المرور كان في `/account/security` بعيداً عن الصفحة.

الآن في **الإعدادات** نموذج: كلمة المرور الحالية، الجديدة، والتأكيد. يستخدم `POST /api/auth` (`changePassword`) ويلغي الجلسات الأخرى.

**مدمج في `main`:** [#50](https://github.com/ainoamn/WAZEN/pull/50). بعد النشر: لوحة المستخدم → الإعدادات → تغيير كلمة المرور.

## تنقّل فوري داخل الحساب

روابط الشريط الجانبي كانت `<a href>` فتعيد تحميل الموقع بالكامل، تُمسح الذاكرة، ويظهر شعار وازن لـ 10–50 ثانية.

الإصلاح: `Link` + تخزين مسبق لـ `/home` `/dashboard` `/billing` `/documents` `/pricing` `/admin`. نموذج الدخول يظهر فوراً دون شاشة الشعار. بعد الدخول تُملأ الذاكرة ثم تُفتح الرئيسية من الكاش.

**مدمج في `main`:** [#48](https://github.com/ainoamn/WAZEN/pull/48). بعد نشر Vercel: التنقّل داخل الحساب يفتح الصفحة فوراً دون شعار كامل. أول زيارة لكل صفحة قد تنتظر البيانات مرة؛ بعدها من الذاكرة.

## فلاش «تعذر تحميل البيانات» قبل تسجيل الدخول

زر **تسجيل الدخول** في الرأس التسويقي كان يفتح `/home`. الصفحة ترسم خطأ التحميل ثم تحوّل إلى `/login`.

الإصلاح: الرابط → `/login`، و«ابدأ مجاناً» → `/register`. `/home` و`/dashboard` بلا كعكة جلسة يُحوَّلان من `proxy` ومن الصفحة. عميل الرئيسية يُبقي شاشة التحميل عند 401 ولا يعرض خطأ البيانات.

**مدمج في `main`:** [#47](https://github.com/ainoamn/WAZEN/pull/47). بعد نشر Vercel: تسجيل الدخول يفتح `/login` مباشرة دون وميض الرئيسية.

آخر إصلاح وظيفي لجوجل: [#41](https://github.com/ainoamn/WAZEN/pull/41) (كعكة الحالة بعد اختيار الحساب). التوثيق: [#39](https://github.com/ainoamn/WAZEN/pull/39) · [#40](https://github.com/ainoamn/WAZEN/pull/40) · [#42](https://github.com/ainoamn/WAZEN/pull/42).

## `ERR_TIMED_OUT` على wazen.bhd-om.com

آخر التحديثات لم تُوقف الموقع. من الخادم: `https://wazen.bhd-om.com` يرجع 200، والقاعدة جاهزة. الخطأ من شبكة عُمان إلى عناوين Vercel `64.29.17.1` / `216.198.79.1`.

افتح فوراً: https://wazen-roan.vercel.app  
في Hostinger غيّر CNAME لـ `wazen` إلى `cname.vercel-dns.com` ثم امسح كاش DNS. التفاصيل: [PRODUCTION-DEPLOYMENT.md](./PRODUCTION-DEPLOYMENT.md).

## تشغيل جوجل في الإنتاج

الزر صار مثل حسابي: Google Identity Services + التحقق من `id_token` في الخادم. **لا يحتاج Client Secret**.

1. Authorized JavaScript origins: `https://wazen.bhd-om.com`
2. نفس معرّف حسابي One BHD (مضمّن كاحتياطي في الكود)
3. Redeploy بعد هذا الدمج ثم `/login`

`GOOGLE_CLIENT_SECRET` اختياري لمسار الإرجاع القديم فقط. التفاصيل: [PRODUCTION-DEPLOYMENT.md](./PRODUCTION-DEPLOYMENT.md).

## تحقق محلي

```powershell
git pull origin main
npm run typecheck
npm run test:backend
```

`npm run lint` ما زال يفشل على ملفات أقدم. لا ترفع ESLint إلى 10.

## مجموعة البرامج

وازن جزء من BHD (`bhd-om.com`). الشقيق حسابي (`hisaby.pro`). رِفد أرشيف تصميم فقط (`archive/rifd`). لا تخلط عملاء OAuth ولا متغيرات أصل التطبيق بين المنتجات. الشرح في التقرير المفصّل القسم 2.

## ما لم يُرفع عمداً

صور شعار بخلفية: `public/brand/*-with-bg.png` (محلية فقط).

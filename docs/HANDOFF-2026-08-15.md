# Handoff — استكمال وازن من أي جهاز

**تاريخ:** 16 أغسطس 2026  
**الفرع:** `main`  
**آخر رفع:** `fc9ef50` (واجهة الجوال: إشعارات ونوافذ داخل الشاشة)  
**المستودع:** https://github.com/ainoamn/WAZEN  
**الإنتاج:** https://wazen-roan.vercel.app  
**قاعدة الإنتاج:** Neon Postgres عبر `DATABASE_URL`

> ابدأ دائماً بـ `git pull --ff-only origin main`. لا تستخدم force push على `main`.  
> بعد كل إنجاز: commit + push إلى `main`.

المرجع السابق: [HANDOFF-2026-08-14.md](./HANDOFF-2026-08-14.md). الأساس: [PRODUCT-FOUNDATION.md](./PRODUCT-FOUNDATION.md).

---

## 1) فتح المشروع على الجهاز الثاني

المتطلبات: **Node.js 22.x** (مثبّت في `package.json`. Vercel يستخدم 22 وليس 24).

```powershell
git clone https://github.com/ainoamn/WAZEN.git
cd WAZEN
git checkout main
git pull --ff-only origin main
npm install
copy .env.example .env.local
# املأ الأسرار من Vercel / Neon — لا تضعها في Git
npm run setup:local
npm run dev
```

الجداول الجديدة (`space_links`, `space_bank_links`) تُنشأ عند أول طلب عبر `ensureWalletLinkTables()` ثم `ensureSchema` (`SCHEMA_VERSION = 3` في `db/runtime.ts`). ملف الهجرة: `drizzle/0006_space_links.sql`.

اختبارات وبناء:

```powershell
npm run test:backend
npm run build
```

النشر: الدفع إلى `main` يشغّل Vercel تلقائياً.

---

## 2) إصلاح بناء Vercel

فشل `ccd78bd` / `0ef488c` / `c7472f5` على TypeScript:

1. `DateField` غير مستورد في `household-family.tsx` (أُصلح في `c7472f5`).
2. مفاتيح مكررة في `dashboardError` (`WALLET_NOT_LINKED` مرتين) → `TS1117`.
3. حذف نوع `Locale` بالخطأ من `personal-wallet.tsx` → `TS2304`.

بعد `b68c689`: `tsc --noEmit` يمر محلياً. لا تبنِ على `ccd78bd` أو `0ef488c` أو `c7472f5`.

---

## 2.1 سلسلة الـ commits المرفوعة (الأحدث أولاً)

| SHA | الرسالة |
|-----|---------|
| `fc9ef50` | `fix: keep mobile overlays, notifications, and modals inside the viewport` |
| `3672278` | `docs: explain dashboard load failure from missing link tables` |
| `f1a7031` | `fix: create wallet link tables on existing production databases` |
| `801184b` | `docs: complete 15 Aug handoff with SHAs and Vercel typecheck status` |
| `5ae3ffb` | `docs: record remaining Vercel typecheck failures` |
| `b68c689` | `fix: pass Vercel typecheck for wallet links and Locale type` |
| `c7472f5` | `fix: restore DateField import so Vercel typecheck can pass` |
| `0ef488c` | `feat: link personal bank accounts to other wallets without mixing funds` |
| `ccd78bd` | `feat: add collapse and expand to every wallet section` |
| `1bb51ea` | `fix: month totals count posted cash only, not pending bills` |
| `be79c2d` | `fix: show an editable paid amount field on monthly ledger items` |
| `8da28d4` | `fix: reopen monthly bills for approve defer or skip after void` |

## 2.2 تعذر تحميل البيانات على الإنتاج (16 أغسطس)

اللوحة فتحت لكن `/api/dashboard` فشل: `SCHEMA_VERSION` رُفع إلى 2 على قاعدة موجودة فاعتُمد الإصدار **دون إنشاء** جدولي `space_links` / `space_bank_links`، ثم الاستعلام منهما أسقط التحميل.

الإصلاح: إنشاء الجدولين دائماً بـ `CREATE TABLE IF NOT EXISTS` قبل فحص الإصدار، وعزل استعلام الربط حتى لا يكسر اللوحة. `SCHEMA_VERSION = 3`.

---

## 2.3 واجهة الجوال — إشعارات ونوافذ مقصوصة (16 أغسطس)

**المشكلة:** على الهاتف كانت لوحات الإشعارات وقائمة الحساب وقوائم الإجراءات وتقويم التاريخ والتنبيهات تظهر ناقصة أو خارج الشاشة.

**الأسباب:**

1. `html`/`body` كانا يستخدمان `overflow-x: clip`، وهذا يقصّ العناصر المنبثقة (وحتى `position: fixed` في بعض المتصفحات إذا كان القص على العنصرين معاً).
2. اللوحات كانت `position: absolute` تحت الشريط العلوي (`z-index: 20`) بعرض ثابت (~360px) فتمتد خارج الحافة في RTL.
3. التنبيه (`.toast`) استخدم `inset-inline-start: 50%` مع `translateX(-50%)` وهذا غير موثوق مع العربية ويخرج النص من الشاشة.
4. عند عرض ≤ 620px كان `.page-content` و`.main-shell` يستخدمان `overflow-x: hidden` فيقصّان القوائم تحت الشريط.

**الإصلاح (`fc9ef50`) في `app/globals.css` و`app/commercial.css`:**

- `html`: `overflow-x: hidden` فقط؛ `body`: `overflow-x: visible` حتى لا تُقصّ الطبقات الثابتة.
- الشريط العلوي: `overflow: visible`.
- على عرض ≤ 900px: الإشعارات وقائمة الحساب `position: fixed` بعرض الشاشة مع هوامش 12px و`max-height` بـ `dvh` + `safe-area`.
- قوائم الإجراءات وتقويم التاريخ: طبقة سفلية ثابتة داخل الإطار.
- النوافذ: حشوة `safe-area`، ارتفاع `min(92dvh, …)`، رأس لاصق، تمرير داخل البطاقة.
- التنبيه: `inset-inline: 16px` بدون إزاحة أفقية، نص يلتف، أسفل الشاشة مع `safe-area-inset-bottom`.
- صفحات التسويق/الدخول/المستندات/الإدارة: عمود واحد على الشاشات الضيقة؛ قائمة التجارة تتمرّر ولا تفيض.

**لا تُرجع:** قصّ أفقي على `body` + `html` معاً، ولا `overflow-x: hidden` على `.page-content`/`.main-shell`، ولا توسيط التنبيه بـ `translateX(-50%)`.

**تحقق يدوي بعد النشر:** جرس الإشعارات، قائمة الحساب، قائمة ⋯ للشهر، نافذة إضافة عملية، تقويم التاريخ، تنبيه حفظ، قائمة الهبوط، الدخول، المستندات، الإدارة — على عرض هاتف (~390px).

---

## 3) ما اكتمل في 15 أغسطس 2026

### طي وفتح أقسام المحفظة

- مكوّن: `components/ui/collapsible-panel.tsx` (`CollapsiblePanel`, `FoldWrap`).
- الحالة تُحفظ في `localStorage` تحت المفتاح `wazen-fold`.
- يغطي: رأس المحفظة، الملخص، التنبؤ، الحسابات، حساب الأشهر (وكل شهر)، الدخل الآخر، الأقساط، صندوق المنزل، المصروفات، الأعضاء، العمليات، الفترات، نظرة عامة (أحدث العمليات / الالتزامات).
- أنماط: `app/globals.css` (`.fold-wrap`, `.panel-fold`, `.is-collapsed`).

### ربط المحافظ وفصل الأموال (المحفظة الشخصية)

الغرض: من يدير محفظته + محفظة الأطفال + جمعيات على نفس الحساب البنكي يرى **أمواله** منفصلة عن **أموال كل محفظة**.

| إجراء API | الوظيفة |
|-----------|---------|
| `linkWallet` | ربط محفظة/جمعية بالمحفظة الشخصية |
| `unlinkWallet` | فصل الربط (يحذف أيضاً ربط البنك) |
| `setWalletBankLink` | تعيين أو إزالة الحساب البنكي الذي تُحفظ عليه أموال المحفظة المرتبطة |
| `transferLinkedFunds` | تحويل `to_linked` (من أموالك إلى المحفظة) أو `to_hub` (العكس) |

الجداول:

- `space_links`: `hub_space_id` (شخصية) + `linked_space_id`
- `space_bank_links`: المحفظة المرتبطة + `account_id` من `personal_accounts`

العرض على بطاقة الحساب: **أموالك** = رصيد الحساب الشخصي؛ **كل محفظة مربوطة** = `balance_minor` لتلك المحفظة؛ **المجموع** = ما يُفترض أنه في البنك.

منطق الفصل: `lib/wallet-links.ts` (`bankCustodySplit`).  
الواجهة: قسم «ربط المحافظ» في `components/personal/personal-wallet.tsx`.

سلوك التحويل: مصروف على المصدر + دخل على الوجهة، مع `account_id` على حركة الحساب الشخصي. لا يُسمح بتحويل أكبر من رصيدك (أو رصيد المحفظة المرتبطة عند الاتجاه العكسي).

### سلوك سابق لم يُعكس (ما زال سارياً)

- إلغاء عملية من أحدث العمليات يبقيها ملغاة في السجل ويعيد بند الشهر إلى معلّق (اعتماد / تأجيل / تجاهل).
- إجمالي الشهر يحسب الدخل/الصرف **المرحّل فقط**؛ المعلّق يظهر ملاحظة ولا يُخصم من الصرف حتى الاعتماد.
- حقل «المدفوع» قابل للتعديل على بنود الشهر.

---

## 4) ملفات لمسها العمل الأخير

| ملف | لماذا |
|-----|--------|
| `components/household/household-family.tsx` | إصلاح استيراد `DateField` |
| `components/ui/collapsible-panel.tsx` | الطي/الفتح |
| `components/personal/personal-wallet.tsx` | الطي + ربط المحافظ |
| `app/wazen-dashboard.tsx` | طي أقسام المحفظة + تمرير الروابط |
| `app/api/dashboard/route.ts` | إجراءات الربط والتحويل |
| `db/runtime.ts` | جداول الربط + `SCHEMA_VERSION` 2 |
| `drizzle/0006_space_links.sql` | هجرة |
| `lib/wallet-links.ts` | حساب فصل الأموال |
| `app/globals.css` | أنماط الطي والربط + طبقات الجوال (إشعارات/نوافذ/تنبيهات) |
| `app/commercial.css` | تخطيط الجوال للتسويق والدخول والمستندات والإدارة |
| `tests/finance.test.mjs` | اختبار `bankCustodySplit` |

---

## 5) قواعد لا تكسرها

| قاعدة | التفصيل |
|--------|---------|
| المبالغ | ريال عماني بوحدات صغرى × **1000** |
| الكتابة | CSRF + idempotency على POST |
| Windows | PowerShell: لا `&&` bash ولا heredoc |
| الهوية | لا ترفع `.env` |
| Node | 22.x في Vercel حتى لو إعداد المشروع 24 |
| الجوال | الطبقات المنبثقة `fixed` داخل الشاشة؛ لا تقصّ `html`+`body` أفقياً معاً |

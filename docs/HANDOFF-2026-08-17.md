# Handoff — 17 أغسطس 2026

**الفرع الرئيسي:** `main` (يشمل [#10](https://github.com/ainoamn/WAZEN/pull/10) و[#11](https://github.com/ainoamn/WAZEN/pull/11))  
**واجهة مقارنة الباقات:** [#12](https://github.com/ainoamn/WAZEN/pull/12) — فرع `cursor/plan-studio-ui-b4f9` (لم يُدمج بعد)  
**المستودع:** https://github.com/ainoamn/WAZEN  
**الإنتاج:** https://wazen-roan.vercel.app  
**الطلبات المدمجة هذا اليوم:** [#10](https://github.com/ainoamn/WAZEN/pull/10) شارة الترقية · [#11](https://github.com/ainoamn/WAZEN/pull/11) حدود الاشتراك  
**واجهة مقارنة الباقات:** [#12](https://github.com/ainoamn/WAZEN/pull/12) — فرع `cursor/plan-studio-ui-b4f9`

المرجع السابق: [HANDOFF-2026-08-16.md](./HANDOFF-2026-08-16.md).

على الجهاز الآخر لـ `main`: `git pull origin main` ثم `npm install` إن لزم.  
لجدول مقارنة الباقات قبل الدمج: `git fetch origin cursor/plan-studio-ui-b4f9 && git checkout cursor/plan-studio-ui-b4f9`. لا ترفع ملفات `public/brand/*-with-bg.png`.

## ماذا أُنجز

### القائمة الجانبية بعد تخفيض الباقة

تخفيض الباقة من **الأعمال** إلى **البداية** كان يخفي المنزل والسفر والجمعيات والأعضاء والتقارير. الآن القائمة تبقى كاملة.

- البنود غير المشمولة تحمل شارة **ترقية**.
- الضغط يفتح ملاحظة: «لاستخدام هذا الخيار رقِّ الباقة إلى العائلة / الاحتراف أو أعلى».
- زر **ترقية الباقة** يفتح `/pricing`.
- واجهة البرمجة ترفض الإنشاء والاستخدام (`PLAN_FEATURE_REQUIRED`).
- محفظة موجودة من نوع لم يعد مشمولاً تبقى قابلة للفتح.

الملفات: `lib/plan-features.ts` (`dashboardNavLocked`, `upgradeNoticeFor`)، `app/wazen-dashboard.tsx`.

### أحدث العمليات

العرض الافتراضي **5**. خيارات 10 / 50 / 100. حد أقصى 100. `lib/transaction-page.ts`.

### تصفية وتصفير على الجوال

الزر على شريط أدوات المحفظة الشخصية، وصندوق التصفير ثابت أسفل نافذة الضبط.

### ميزات الباقة من لوحة المستخدم والفواتير

كتالوج الميزات ظاهر (مشمولة / ترقية). تصدير JSON يحتاج `exports`. كشوف المستندات المتقدمة تحتاج `documents`؛ إيصال قبض بسيط يبقى متاحاً.

## تحقق محلي

```powershell
git fetch origin cursor/plan-studio-ui-b4f9
git checkout cursor/plan-studio-ui-b4f9
npm run typecheck
npm run test:backend
```

بعد دمج [#12](https://github.com/ainoamn/WAZEN/pull/12) استخدم `git pull origin main` بدل الفرع أعلاه.

`npm run lint` ما زال يفشل على `main` من قبل هذا التسليم (jsx-a11y وreact-hooks في ملفات أقدم). لا ترفع ESLint إلى 10.

## صفحة الباقات والحدود

البيانات والفرض من [#11](https://github.com/ainoamn/WAZEN/pull/11) (`SCHEMA_VERSION = 6`). الواجهة في [#12](https://github.com/ainoamn/WAZEN/pull/12): جدول مقارنة قابل للتحرير، الباقات أعمدة والصفوف عناصر التحكم.

### مقارنة `/admin/plans` وتعديلها

- الصف الأول «العناوين»: عمود ثابت لأسماء الصفوف.
- رأس كل عمود: اسم الباقة، المعرّف، السعر، عدد المشتركين، مفتاح النشطة، وزر حفظ.
- الصفوف بالترتيب: هوية الباقة (معرّف، أسماء، وصف، ترتيب) → التسعير (شهري/سنوي ر.ع) → حدود الاشتراك الخمسة → مجموعات الخصائص (محافظ، تقارير ومستندات، أدوات ودعم) → بوابات الدفع صفاً لكل بوابة.
- باقة جديدة = عمود جديد من «عمود باقة جديدة». المعرّف يُقفل بعد أول حفظ. التغييرات غير المحفوظة شريط أخضر على الرأس، وزر «حفظ» في العمود أو «حفظ التغييرات» أعلى الصفحة.
- العمود الثالث (الاحتراف عادةً) مميّز بشريط «الأوسع استخداماً».
- الأنماط: `app/commercial.css` (`.plan-matrix`). المنطق: `app/admin/admin-plans-client.tsx` (`upsertPlan` كما في #11).

تحقق سريع: افتح `/admin/plans` — أربعة أعمدة لا بطاقات ولا نموذج منفصل. غيّر حد المعاملات في العائلة ومفتاح خاصية ثم احفظ العمود.

لكل باقة عند الاشتراك يُضبط:

| الحد | المعنى | غير محدود |
|------|--------|-----------|
| المحافظ | عدد المحافظ | `9999` |
| الأعضاء | سجلات الأعضاء داخل المحفظة | `9999` |
| المستخدمون | حسابات الدخول المرتبطة بالاشتراك | `0` |
| المعاملات | الحركات في محافظ المشترك | `0` |
| السجلات | المستندات والإيصالات | `0` |

المخطط `SCHEMA_VERSION = 6`. واجهة البرمجة ترفض التجاوز: `PLAN_TRANSACTION_LIMIT` / `PLAN_RECORD_LIMIT` / `PLAN_USER_LIMIT`. بذور الباقات: البداية 50/20/1، العائلة 300/100/5، الاحتراف 2000/500/25، الأعمال غير محدود.

الملفات: `app/admin/admin-plans-client.tsx`، `app/commercial.css`، `lib/plan-features.ts`، `services/admin/billing-service.ts`، `db/runtime.ts`، `drizzle/0007_plan_quotas.sql`.

## ما لم يُرفع عمداً

صور شعار بخلفية: `public/brand/*-with-bg.png` (محلية فقط).

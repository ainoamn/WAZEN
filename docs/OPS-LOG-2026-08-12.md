# سجل عمليات وازن — 12 أغسطس 2026

## قرار التثبيت

تم اعتماد تقرير `WAZEN-DEVELOPMENT-RESET-ADMIN-PLAN-2026-08-12.md` كمرجع تنفيذي داخل المستودع، مع خطة backlog قابلة للعمل في `EXECUTION-BACKLOG-2026-08-12.md`.

## إصلاح نشر Vercel (حرج) — نجح

| المشكلة | السبب | الإصلاح |
|---------|--------|---------|
| فشل كل نشر بعد v0.2.0 (~33ث) | `output:"standalone"` + محوّل Vercel على Next 16.3 → `ENOENT next-server.js.nft.json` | تعطيل standalone عند `VERCEL=1`؛ الإبقاء عليه لـ Docker |
| تحذير engines | `>=22.13.0` | تثبيت `22.x` |
| `/api/health` كان 404 | بناء قديم فاشل | **صار يعمل** على `https://wazen-roan.vercel.app/api/health` |

- Commit الإصلاح: `75bd684`
- Production Ready بعد إعادة النشر
- الحالة الحالية للـ health: `degraded` / `database: unavailable` لأن **Turso غير مربوط** (أسرار التطبيق أُضيفت؛ ينقص `TURSO_*` فقط)

## الخطوة الفورية التالية (مانع الإنتاج)

1. إنشاء قاعدة Turso باسم `wazen-production-v1`
2. إضافة `TURSO_DATABASE_URL` + `TURSO_AUTH_TOKEN` إلى Vercel (باقي الأسرار أُضيفت: Demo=0، Admin email، keyring، webhook/job secrets)
3. `npm run db:migrate`
4. `npm run admin:bootstrap -- --email admin@wazen.pro --name "Wazen Admin" --origin https://wazen-roan.vercel.app`
5. فتح رابط التهيئة، كلمة مرور ≥12، ثم TOTP

## شعار وهوية بصرية — 12 أغسطس 2026

- تصميم شعار وازن (علامة و + تدرج زمردي + لمسة ميزان).
- أصول: `public/brand/wazen-mark.svg|png` و`wazen-lockup.svg` و`public/favicon.svg`.
- دمج في `Brand`، لوحة المستخدم، الإدارة، وmetadata.
- دليل الهوية: `docs/BRAND.md`.

## تهيئة الإنتاج بنقرة واحدة (بعد توكن Turso)

```powershell
$env:TURSO_API_TOKEN="your-platform-api-token"
$env:TURSO_ORG="your-org-slug"
npm run provision:production
```

السكربت ينشئ قاعدة `wazen-production-v1`، يضبط متغيرات Vercel، يشغّل الهجرات، يصدر رابط `/admin/setup` لـ `admin@wazen.pro`، ويعيد النشر.

## ما نُفّذ سابقاً في الكود

تقرير، backlog، حماية Demo، bootstrap/reset، صفحات admin users/tenants، secrets helper — انظر Git history من `b5fdfbd` إلى `75bd684`.

# هوية وازن البصرية (Brand)

تاريخ الاعتماد: 12 أغسطس 2026 (تحديث الشعار)

## الاسم

- عربي: **وازن**
- لاتيني: **WAZEN**
- لا تُكتب Wazen Finance كاسم أساسي في الواجهة؛ الاسم القصير يكفي.

## الشعار

| الملف | الاستخدام |
|-------|-----------|
| `public/brand/wazen-mark.svg` | العلامة الأساسية في الواجهة (مفضّل) |
| `public/brand/wazen-mark.png` | أيقونة/Apple touch والنسخ التسويقية |
| `public/brand/wazen-lockup.svg` | الاسم الكامل مع العلامة (عروض وصفحات) |
| `public/favicon.svg` | أيقونة المتصفح |

### الفكرة (v2)

- **ميزان توازن** هندسي بسيط — عمود، ذراع أفقي، كفّتان دائريتان متساويتان.
- نقطة محور بلون `#B8F0DE` ترمز للتوازن دون ازدحام.
- حاوية بزاوية سفلية يسارية مقطوعة (تطابق `.brand-mark` و`.commerce-brand`).
- تدرج زمردي من `#123C36` إلى `#0A7A64`.

### ما تغيّر عن v1

- إزالة فقاعة الدردشة وحرف «و» المشوّه.
- شكل أوضح يقرأ كـ «توازن مالي» حتى في 32px.

### ممنوع

- البنفسجي الافتراضي لتصاميم AI، التوهج النيوني، الكروم ثلاثي الأبعاد.
- خلفية كريمية + تراكوتا كاتجاه بديل للهوية.
- إضافة أيقونات عملات أو رموز بنكية فوق الشعار.
- فقاعات محادثة أو حروف عربية مشوّهة داخل الأيقونة.

## الألوان

| الدور | القيمة |
|-------|--------|
| Primary deep | `#123C36` |
| Primary accent | `#0A7A64` |
| Soft mint | `#E8F5F1` / `#B8F0DE` |
| Ink | حسب `--ink` في CSS |
| Muted | `#7d8c86` |

## الاستخدام في الكود

- مكوّن `Brand` في `app/commercial-kit.tsx` يستخدم `wazen-mark.svg`.
- لوحة المستخدم والإدارة تعرض نفس العلامة داخل `.brand-mark`.
- البيانات الوصفية في `app/layout.tsx` تشير إلى favicon والـ PNG.
- صورة المشاركة الاجتماعية تُولَّد ديناميكياً عبر `app/opengraph-image.tsx`.

## ملاحظات إنتاج

- لا تغيّر نسب العلامة؛ حافظ على مساحة أمان ≈ 20% حول الأيقونة.
- على الخلفيات الداكنة استخدم النسخة الفاتحة من النص فقط، والعلامة كما هي.

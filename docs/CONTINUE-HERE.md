# استئناف العمل من جهاز آخر — جلسة سبتمبر 2026

**اقرأ هذا الملف أولاً على الكمبيوتر الثاني.**

| | |
|--|--|
| **الفرع** | `main` |
| **المستودع** | https://github.com/ainoamn/WAZEN |
| **آخر عمل** | 18 سبتمبر 2026 — مبلغ الصندوق يقبل الكسور (7.500) عند الدفع صندوق وعضو |
| **الإنتاج** | https://wazen.bhd-om.com |
| **تفاصيل 11 سبتمبر** | [HANDOFF-2026-09-11.md](./HANDOFF-2026-09-11.md) |
| **تفاصيل 9 سبتمبر** | [HANDOFF-2026-09-09.md](./HANDOFF-2026-09-09.md) |
| **جلسة 1 سبتمبر** | [HANDOFF-2026-09-01-SESSION.md](./HANDOFF-2026-09-01-SESSION.md) |

---

## أوامر على الجهاز الثاني

```bash
cd /path/to/WAZEN
git pull origin main
npm install
```

ثم افتح بالترتيب:

1. **هذا الملف** (`docs/CONTINUE-HERE.md`)
2. **[HANDOFF-2026-09-11.md](./HANDOFF-2026-09-11.md)** — مدفوع الرحلة + هدف غير محصّل + محفظة بلا مساهمة
3. **[WALLET-ACCOUNTING.md](./WALLET-ACCOUNTING.md)** — قواعد عليه/له/مدفوع لكل نوع محفظة
4. **[HANDOFF-2026-09-09.md](./HANDOFF-2026-09-09.md)** — تكرار / دمج / جهات اتصال
5. **[HANDOFF-2026-09-01-SESSION.md](./HANDOFF-2026-09-01-SESSION.md)** — بريد الكشف والخروج وواتساب

---

## ماذا تم (ملخص)

| العمل | الحالة |
|--------|--------|
| محاسبة عليه/له · إصلاح الخروج · واتساب/SMS · بريد الكشف | ✅ على `main` ومنشور |
| منع تكرار **رقم الهاتف** داخل الجمعية (البريد مسموح) + زر تحرير البيانات | ✅ |
| أداة دمج المكررات داخل الجمعية فقط (زر **دمج المكرر** ظاهر دائماً) | ✅ |
| توضيح عمود الجمعيات + توحيد يدوي لشخص برقمين مختلفين | ✅ على `main` ومنشور (`ad8a31a`) |
| زر **تعديل** ظاهر في جدول الأعضاء لتغيير الاسم/الهاتف/البريد | ✅ على `main` ومنشور (`93c68c1`) |
| طباعة/واتساب الكشف: كامل لكل الجمعيات أو جمعية معينة | ✅ على `main` ومنشور (`cbbf87a`) |
| حقول صفحة المجموعات مقروءة في الوضع المظلم (بحث/تعديل/كشف) | ✅ على `main` ومنشور (`f3bea9c`) |
| كشف العضو: جدول لكل جمعية، واتساب بنفس مبالغ الملف، زر طباعة أعلى الصفحة | ✅ على `main` ومنشور (`de608b3`) |
| تسويات الرحلة: صافي الأعضاء (مبالغ كبيرة فرداً لفرد) بدل تقسيم كل فاتورة | ✅ على `main` ومنشور (`b50f82d`) |
| المحفظة الشخصية: أقساط/فواتير متكررة + تذكير قبل يوم وفي يوم الدفع (تطبيق، بريد، واتساب) | ✅ على `main` ومنشور (`df5c1e0`) |
| تسوية الرحلة تُحفظ كتحويل محاسبي (من → إلى · السبب · كيف · مدفوع/متبقي) | ✅ على `main` ومنشور (`be6c428`) |
| المحفظة الشخصية: دخل شهري ثابت + تصنيفات + تقرير توفير/ثغرات + تذكير للدخل والمصروف | ✅ على `main` ومنشور (`52d4587`) |
| محفظة الرحلة: هدف الادخار ليس ديناً · المصروف من الجيب · التحويلات ليست دخلاً | ✅ على `main` ومنشور (`196907b`) |
| المحفظة الشخصية: أبواب نظرة عامة / دخل / مصروف / أشهر / معاملات مع تلوين أخضر وأحمر | ✅ على `main` ومنشور (`196907b`) |
| رحلة: عمود **سدّد للأعضاء** من التحويلات المسوّاة + اختبار ذهبي لبندر + [WALLET-ACCOUNTING.md](./WALLET-ACCOUNTING.md) | ✅ على `main` ومنشور (`a837074`) ثم صُحّح |
| رحلة: عمود **مدفوع** = حصة كل عضو من الفواتير (بندر: ~13.8 لكل فرد، إجمالي 82.801) | ✅ على `main` ومنشور (`3aedc3d`) |
| كشف العضو: بنود الدفع خضراء مع شارة داكنة **دفع** | ✅ على `main` ومنشور (`81e95ff`) |
| رحلة بصندوق: **مدفوع** = المساهمة لا الحصص (أذربيجان 200+200=400) + كاش الجلسة بدل شعار طويل | ✅ على `main` ومنشور (`e1a9514`) |
| هدف الرحلة غير المحصّل لا يظهر في الكشف؛ يظهر بعد التحصيل وفي التذكيرات. فتح محفظة بلا مساهمة شهرية | ✅ على `main` ومنشور (`c05999a`) |
| PWA: فتح الصفحات من الجهاز بدون شبكة، تحديث ورفع في الخلفية دون إعادة تحميل عند كل فتح | ✅ على `main` ومنشور (`22edf58`) |
| كشف عضو الرحلة = الجدول: مدفوع الصندوق، صرف الجيب، عجز واحد، تقدم الهدف من المحصّل | ✅ على `main` ومنشور (`3cf30d5`) |
| صرف الرحلة = حصص الفواتير لا صفر (أذربيجان 218.422) مع إبقاء المدفوع 200 | ✅ على `main` ومنشور (`31ef8f1`) |
| كشف الحساب التفصيلي: الرصيد السالب بين قوسين وبالأحمر | ✅ على `main` ومنشور (`ce859bf`) |
| كشف العضو وباقي الموقع: الصرف وعليه والسوالب بنفس الأسلوب | ✅ على `main` ومنشور (`1ba867f`) |
| رسالة الكشف: حول إلى فلان حسب التقسيمة + اعتماد المدير/الصندوق أو تأكيد المستلم | ✅ على `main` ومنشور (`ff393f7`) |
| مصروف جماعي: اختيار من يتحمل الفاتورة (شخصي / عن غيره / على مجموعة) دون إعادة قسمة القديم على المنضم | ✅ على `main` ومنشور (`426551d`) |
| مصروف واحد: مبلغ مشترك على المحددين + مبالغ إضافية لعضو أو أكثر (331 + 197.450) | ✅ على `main` ومنشور (`d6ce76a`) |
| اختيار من يتحمل المصروف: **الكل** أو **اختيار من القائمة** مع بحث وعلامة صح (بدل البطاقات) | ✅ على `main` ومنشور (`033a63d`) |
| مصروف مبسّط: مبلغ كلي → الكل، أو مبلغ على الجميع ثم المتبقي يُختار أعضاؤه | ✅ على `main` ومنشور (`480fcdf`) |
| رحلة الصندوق: **مدفوع** نقد المساهمة فقط (ماجد ٠ لا 109) و**عليه** = حصص − مدفوع | ✅ على `main` ومنشور (`9bce369`) |
| تعديل مصروف الرحلة: التقسيمة المحفوظة (ماجد وحده لا الكل) + تغيير **دُفع من** | ✅ على `main` ومنشور (`749cf0d`) |
| رحلة: شرح مدفوع/له في الجدول + دفع فاتورة **صندوق وعضو** | ✅ على `main` ومنشور (`c313357`) |
| الصندوق لا يصير سالباً: فاتورة فوق النقد تُجبر تقسيماً مع اختيار من دفع المتبقي | ✅ على `main` ومنشور (`77f11a9`) |
| مبلغ الصندوق/العضو يقبل ثلاث خانات عشرية أثناء الكتابة (لا يُثبَّت على رقم صحيح) | ⏳ يُنشر الآن |
| دعوة: من الهاتف أو **من Gmail** مباشرة أو رفع/تنزيل vCard وCSV | ✅ |
| Next.js 16.3.4 لتمرير `npm audit` ونشر Vercel | ✅ |

**الإنتاج:** https://wazen.bhd-om.com — `buildId` الحي: `77f11a9af3b5c00020189d05ee1179efc444b482`

Vercel كان يرفض `8aaedce` لأن Hobby لا يسمح بـ cron كل 5 دقائق. `426551d` يُشغّل `/api/jobs/tick` مرة يومياً في 02:00 و03:00 و06:00 و14:00 UTC. CI `verify` ما زال يفشل على lint قديم؛ Vercel لا ينتظره.

بطاقة Vercel **Error** على `c402e5c` (أقواس الصرف/عليه) و`b94b94e` (كشف العضو) و`f435d97` (تحديث القاعدة) ليست الإنتاج الحي: فشل TypeScript ثم صُحّح في `1ba867f`. لا تعِد نشر تلك الالتزامات.

---

## ما لم يُرفع عمداً

- `public/brand/*-with-bg.png` — ملفات خلفية محلية؛ لا تُرفع إلا بطلب صريح.

---

## ما يمكن استكماله لاحقاً

- [ ] ضبط `WHATSAPP_*` / SMS / `RESEND_*` على Vercel إن لم تُضبط
- [ ] اختبار يدوي: معاملة جماعية → وصول بريد للأعضاء
- [ ] بريد كشف من بطاقة العضو
- [ ] Contact Picker غير متاح في بعض المتصفحات (Safari) — استخدم **من Gmail** أو رفع الملف
- [x] نطاق Google Contacts (`contacts.readonly`) على شاشة موافقة OAuth إن رُفض زر «من Gmail» — أضف المختبرين في Audience؛ التحقق الكامل مطلوب للعامة

---

## رسالة جاهزة للوكيل على الجهاز الثاني

> اسحب `main` ثم اقرأ `docs/CONTINUE-HERE.md` و`docs/HANDOFF-2026-09-11.md`.  
> لا تعِد تنفيذ المحاسبة أو الخروج أو بريد الكشف أو منع التكرار أو دمج المكررات أو استيراد الجهات أو توحيد الشخص أو تصفية تسويات الرحلة أو تقسيم المصروف حسب الأشخاص أو التقسيم المشترك+الإضافي أو قائمة اختيار الأعضاء بعلامة صح — كلها مدمجة.  
> أنشر وأوثّق وأدمج على `main` دون سؤال بعد كل إصلاح حي.  
> إن طُلب اختبار: محفظة بندر بعد التسوية **مدفوع ≈ 13.8 لكل عضو** و**إجمالي المدفوع 82.801** و**عليه/له = ٠**. أذربيجان: ماجد **مدفوع ٠** و**عليه = حصصه فقط** (لا يُزاد الهدف غير المدفوع). تعديل «تذكره اذربيجان جورجيا» يفتح **اختيار من القائمة** على ماجد ويمكن تغيير **دُفع من**. المحفظة الشخصية `/dashboard?view=personal`: أبواب **نظرة عامة / الدخل / المصروف / الأشهر / المعاملات**.

---

*آخر تحديث: 17 سبتمبر 2026*

# استئناف العمل من جهاز آخر — جلسة سبتمبر 2026

**اقرأ هذا الملف أولاً على الكمبيوتر الثاني.**

| | |
|--|--|
| **الفرع** | `main` |
| **المستودع** | https://github.com/ainoamn/WAZEN |
| **آخر عمل** | 9 سبتمبر 2026 — دخل ومصروف شهري ثابت + تصنيفات + تقرير توفير في المحفظة الشخصية |
| **الإنتاج** | https://wazen.bhd-om.com |
| **تفاصيل 9 سبتمبر** | [HANDOFF-2026-09-09.md](./HANDOFF-2026-09-09.md) |
| **جلسة 1 سبتمبر** | [HANDOFF-2026-09-01-SESSION.md](./HANDOFF-2026-09-01-SESSION.md) |

---

## أوامر على الجهاز الثاني

```bash
cd /path/to/WAZEN
git pull origin main
npm install
```

ثم افتح بالترتيب:

1. **هذا الملف** (`docs/CONTINUE-HERE.md`)
2. **[HANDOFF-2026-09-09.md](./HANDOFF-2026-09-09.md)** — تكرار / دمج / جهات اتصال
3. **[HANDOFF-2026-09-01-SESSION.md](./HANDOFF-2026-09-01-SESSION.md)** — بريد الكشف والخروج وواتساب

---

## ماذا تم (ملخص)

| العمل | الحالة |
|--------|--------|
| محاسبة عليه/له · إصلاح الخروج · واتساب/SMS · بريد الكشف | ✅ على `main` ومنشور |
| منع تكرار **رقم الهاتف** داخل الجمعية (البريد مسموح) + زر تحرير البيانات | ✅ |
| أداة دمج المكررات داخل الجمعية فقط (زر **دمج المكرر** ظاهر دائماً) | ✅ |
| توضيح عمود الجمعيات + توحيد يدوي لشخص برقمين مختلفين | ✅ على `main` ومنشور (`ad8a31a`) |
| زر **تعديل** ظاهر في جدول الأعضاء لتغيير الاسم/الهاتف/البريد | ✅ على `main` ومنشور (`93c68c1`) |
| طباعة/واتساب الكشف: كامل لكل الجمعيات أو جمعية معينة | ✅ على `main` ومنشور (`cbbf87a`) |
| حقول صفحة المجموعات مقروءة في الوضع المظلم (بحث/تعديل/كشف) | ✅ على `main` ومنشور (`f3bea9c`) |
| كشف العضو: جدول لكل جمعية، واتساب بنفس مبالغ الملف، زر طباعة أعلى الصفحة | ✅ على `main` ومنشور (`de608b3`) |
| تسويات الرحلة: صافي الأعضاء (مبالغ كبيرة فرداً لفرد) بدل تقسيم كل فاتورة | ✅ على `main` ومنشور (`b50f82d`) |
| المحفظة الشخصية: أقساط/فواتير متكررة + تذكير قبل يوم وفي يوم الدفع (تطبيق، بريد، واتساب) | ✅ على `main` ومنشور (`df5c1e0`) |
| تسوية الرحلة تُحفظ كتحويل محاسبي (من → إلى · السبب · كيف · مدفوع/متبقي) | ✅ على `main` ومنشور (`be6c428`) |
| المحفظة الشخصية: دخل شهري ثابت + تصنيفات + تقرير توفير/ثغرات + تذكير للدخل والمصروف | ✅ على `main` ومنشور (`52d4587`) |
| دعوة: من الهاتف أو **من Gmail** مباشرة أو رفع/تنزيل vCard وCSV | ✅ |
| Next.js 16.3.4 لتمرير `npm audit` ونشر Vercel | ✅ |

**الإنتاج:** https://wazen.bhd-om.com — `buildId` الحي: `52d4587d8f31109803e9237da0689e5263787a73`

بطاقة Vercel **Error** على `b94b94e` (كشف العضو) و`f435d97` (تحديث القاعدة) ليست الإنتاج الحي: فشل TypeScript ثم صُحّح. لا تعِد نشر تلك الالتزامات.

---

## ما لم يُرفع عمداً

- `public/brand/*-with-bg.png` — ملفات خلفية محلية؛ لا تُرفع إلا بطلب صريح.

---

## ما يمكن استكماله لاحقاً

- [ ] ضبط `WHATSAPP_*` / SMS / `RESEND_*` على Vercel إن لم تُضبط
- [ ] اختبار يدوي: معاملة جماعية → وصول بريد للأعضاء
- [ ] بريد كشف من بطاقة العضو
- [ ] Contact Picker غير متاح في بعض المتصفحات (Safari) — استخدم **من Gmail** أو رفع الملف
- [x] نطاق Google Contacts (`contacts.readonly`) على شاشة موافقة OAuth إن رُفض زر «من Gmail» — أضف المختبرين في Audience؛ التحقق الكامل مطلوب للعامة

---

## رسالة جاهزة للوكيل على الجهاز الثاني

> اسحب `main` ثم اقرأ `docs/CONTINUE-HERE.md` و`docs/HANDOFF-2026-09-09.md`.  
> لا تعِد تنفيذ المحاسبة أو الخروج أو بريد الكشف أو منع التكرار أو دمج المكررات أو استيراد الجهات أو توحيد الشخص أو تصفية تسويات الرحلة — كلها مدمجة.  
> إن طُلب اختبار: صفحة الأعضاء `/dashboard?view=groups` ثم طباعة/واتساب الكشف. محفظة الرحلة: **تصفية التسويات** ثم **تم التسوية** يظهر في **سجل التحويلات**. المحفظة الشخصية `/dashboard?view=personal`: **دخل شهري ثابت** أو **قسط / تمويل** أو **فاتورة شهرية** — النظام يولّد الشهر وأنت تعتمد أو تؤجّل أو تتجاهل.

---

*آخر تحديث: 9 سبتمبر 2026*

# وازن | WAZEN — التوثيق الكامل للمنصة

**الإصدار:** 0.2.0 (`wazen-finance`)
**تاريخ التوثيق الأصلي:** 11 أغسطس 2026  
**آخر تسليم تشغيلي:** 18 أغسطس 2026 — **[docs/HANDOFF-2026-08-18.md](./docs/HANDOFF-2026-08-18.md)** و**[docs/PLATFORM-REPORT-2026-08-18.md](./docs/PLATFORM-REPORT-2026-08-18.md)** (تقرير المنصة والتقنيات والمجموعة؛ السابق: [17 أغسطس](./docs/HANDOFF-2026-08-17.md))  
**المستودع:** https://github.com/ainoamn/WAZEN  
**الفرع الرئيسي:** `main`  
**الإنتاج:** https://wazen.bhd-om.com (نشر Vercel: https://wazen-roan.vercel.app)  
**أرشيف النسخة السابقة (رِفد):** `archive/rifd`  

> لاستكمال التطوير من جهاز جديد استخدم **[docs/HANDOFF-2026-08-18.md](./docs/HANDOFF-2026-08-18.md)** والتقرير **[docs/PLATFORM-REPORT-2026-08-18.md](./docs/PLATFORM-REPORT-2026-08-18.md)**. تقديرات النضج في القسم 1 أدناه تصف حالة أقدم ولا تمثّل المصادقة الحالية (جلسات + جوجل).  
> **مهم:** القواعد والفكرة المؤسسة للمنتج موثّقة في:  
> **[docs/PRODUCT-FOUNDATION.md](./docs/PRODUCT-FOUNDATION.md)**

> **تحديث أمني 0.2.0:** أُغلقت المخاطر الحرجة المذكورة في تدقيق 0.1 التاريخي أدناه: أزيل مستخدم Vercel المشترك، ومُنع SQLite المؤقت في الإنتاج، وأضيفت مصادقة وجلسات وصلاحيات دقيقة وقيود مزدوجة وIdempotency وRate limiting وWebhooks موقعة. المرجع التشغيلي الحالي هو **[docs/PRODUCTION-DEPLOYMENT.md](./docs/PRODUCTION-DEPLOYMENT.md)** والمرجع الأمني هو **[SECURITY.md](./SECURITY.md)**. الأقسام التي تصف `demo auth` و`/tmp` تبقى كسجل تاريخي للنسخة 0.1 وليست وصفاً للكود الحالي.

> **هدف النشر الحالي:** Next.js على Vercel فقط. قاعدة الإنتاج الحالية **Neon Postgres** عبر `DATABASE_URL` (Turso اختياري قديم). راجع [docs/NEON-SETUP.md](./docs/NEON-SETUP.md).

هذا الملف يوثّق الجانب **التقني والتشغيلي**: طريقة عمل الكود، APIs، الأمان الحالي، ما ينقص، وسجل ما تم على Git.

---

## فهرس المحتويات

1. [ملخص تنفيذي](#1-ملخص-تنفيذي)
2. [سجل المعاملات والإنجازات](#2-سجل-المعاملات-والإنجازات)
3. [ما هي المنصة وماذا تقدّم](#3-ما-هي-المنصة-وماذا-تقدّم)
4. [كيف يعمل البرنامج](#4-كيف-يعمل-البرنامج)
5. [الصفحات والواجهات](#5-الصفحات-والواجهات)
6. [واجهات API](#6-واجهات-api)
7. [قاعدة البيانات](#7-قاعدة-البيانات)
8. [التقنيات المستخدمة](#8-التقنيات-المستخدمة)
9. [الإيجابيات](#9-الإيجابيات)
10. [السلبيات](#10-السلبيات)
11. [الحماية والتشفير](#11-الحماية-والتشفير)
12. [الثغرات والمشاكل الفنية](#12-الثغرات-والمشاكل-الفنية)
13. [ما ينقص للإطلاق والتجربة](#13-ما-ينقص-للإطلاق-والتجربة)
14. [اقتراحات التطوير](#14-اقتراحات-التطوير)
15. [هيكل المشروع والمسارات المحلية](#15-هيكل-المشروع-والمسارات-المحلية)
16. [أوامر التشغيل والبناء](#16-أوامر-التشغيل-والبناء)
17. [خارطة الطريق المقترحة](#17-خارطة-الطريق-المقترحة)
18. [ملحق: العلاقة برِفد](#18-ملحق-العلاقة-برِفد)

---

## 1. ملخص تنفيذي

**وازن** منصة مالية عربية (مع دعم الإنجليزية) لإدارة:

- المال الشخصي  
- ميزانيات المنزل والعائلة  
- رحلات المجموعات وتسوياتها  
- الجمعيات الدورية  
- الاشتراكات والفوترة  
- الإيصالات والكشوفات  
- لوحة إدارة تجارية للمنصة (عملاء، باقات، مدفوعات، تقارير)

### حالة النضج الحالية

| المحور | التقدير التقريبي | ملاحظة |
|--------|------------------|--------|
| الواجهات / UX | 70–80% | شاشات غنية، ثنائية اللغة |
| Backend / API | 55–65% | APIs تعمل مع seed وCRUD جزئي |
| قاعدة البيانات | 60% | schema + D1 / SQLite fallback |
| المصادقة والأمان | 15–25% | demo auth / SIWC headers فقط |
| الدفع الحقيقي | 5% | فواتير ومحاكاة، بدون بوابة دفع |
| جاهزية إنتاج مالي | **غير جاهز** | مناسب لـ MVP / تجربة داخلية |

المنصة اليوم: **MVP مبكر قابل للتجربة** وليس منتجاً مالياً مرخّصاً للإطلاق العام.

---

## 2. سجل المعاملات والإنجازات

ما تم تنفيذه خلال جلسات العمل على المشروع:

### 2.1 رفع النسخة الأولى (رِفد) إلى GitHub

| البند | التفاصيل |
|--------|---------|
| المستودع | https://github.com/ainoamn/WAZEN |
| المحتوى الأول | مشروع `C:\dev\WAZEN` (رِفد UI mock) |
| Commits مبكرة | `11b9649` Initial · `fe6db47` Vercel build path لرِفد |

### 2.2 ربط ونشر Vercel (رِفد ثم وازن)

- محاولة استيراد المستودع على Vercel.  
- إصلاح أخطاء سكربتات bash وصلاحيات التنفيذ (نسخة رِفد).  
- بعد اعتماد وازن: فشل البناء لأن `next` غير موجود في `package.json`.  
- إصلاح نهائي: إضافة Next.js + مسار بناء مزدوج + SQLite على Vercel  
  - commit: `2492546` — `fix: enable Vercel builds with next and Node SQLite`

### 2.3 مقارنة المنصتين وقرار الاعتماد

| المشروع | الدور | النتيجة |
|---------|--------|---------|
| `C:\dev\WAZEN` (رِفد سابقاً) | نموذج واجهات فقط | أُرشف |
| `C:\dev\WAZEN-1` (وازن) | MVP بقاعدة بيانات وAPI | **اعتمد كمنصة رئيسية** |

### 2.4 أرشفة رِفد واعتماد وازن على Git

| الفرع / المكان | المحتوى |
|----------------|---------|
| `main` | منصة وازن (من WAZEN-1) |
| `archive/rifd` | أرشيف رِفد للرجوع الأفكار والتصميم |
| محلياً `C:\dev\WAZEN` | worktree الفرع `main` |
| محلياً `C:\dev\WAZEN-archive-rifd` | worktree الفرع `archive/rifd` |
| `C:\dev\WAZEN-1` | حُذف بعد الدمج (كان تكراراً) |

### 2.5 Commits المهمة على `main`

```
2492546  fix: enable Vercel builds with next and Node SQLite
35d440b  docs: document local RIFD archive folder path
fe0cb34  feat: adopt Wazen (WAZEN-1) as main platform; RIFD kept on archive/rifd
fe6db47  fix: support Vercel Next.js build path   ← آخر نقطة في أرشيف رِفد
11b9649  Initial commit: WAZEN project            ← بداية رِفد
```

### 2.6 إصلاح Vercel لوازن (تفاصيل تقنية)

ما أُضيف/غُيّر لدعم النشر:

- حزمة `next@16.2.6` في `dependencies`
- `scripts/build.mjs` — يختار `next build` على Vercel و`vinext build` على Cloudflare Sites
- `vercel.json` — إعداد إطار Next.js
- `lib/cloudflare-workers-stub.ts` + alias في `next.config.ts`
- `db/node-sqlite-d1.ts` — تنفيذ واجهة شبيهة بـ D1 عبر `node:sqlite` على Vercel
- `db/runtime.ts` — اختيار D1 إن وُجد، وإلا Node SQLite
- مستخدم demo تلقائي عند `VERCEL=1` أو localhost

### 2.7 15 أغسطس 2026 — طي الأقسام، ربط المحافظ، إصلاح البناء

التفاصيل التشغيلية: **[docs/HANDOFF-2026-08-15.md](./docs/HANDOFF-2026-08-15.md)**

- طي/فتح أقسام المحفظة مع حفظ الحالة في المتصفح (`ccd78bd`).
- ربط محفظة شخصية بمحافظ/جمعيات أخرى وحساب بنكي، مع فصل «أموالك» عن أموال كل محفظة، وتحويل بينهما (`0ef488c`).
- إصلاح بناء Vercel: استيراد `DateField` (`c7472f5`) ثم مفاتيح `dashboardError` المكررة ونوع `Locale` (`b68c689`). `tsc --noEmit` يمر.
- التسليم: [docs/HANDOFF-2026-08-15.md](./docs/HANDOFF-2026-08-15.md)
- إنتاج: جداول الربط على قواعد قديمة (`f1a7031`, `SCHEMA_VERSION = 3`) حتى لا تفشل `/api/dashboard`.

### 2.8 16 أغسطس 2026 — تناسق الواجهة على الهاتف

التفاصيل: **[docs/HANDOFF-2026-08-15.md](./docs/HANDOFF-2026-08-15.md)** §2.3

- الإشعارات وقوائم الحساب والإجراءات وتقويم التاريخ والتنبيهات تبقى داخل الشاشة (`fc9ef50`).
- النوافذ تستخدم ارتفاع `dvh` ومنطقة آمنة؛ صفحات الدخول والمستندات والإدارة عمود واحد على العرض الضيق.
- الملفات: `app/globals.css`, `app/commercial.css`. `viewportFit: "cover"` موجود في `app/layout.tsx`.

### 2.9 17 أغسطس 2026 — شارة الترقية وحدود الباقات ومقارنة الإدارة

التفاصيل: **[docs/HANDOFF-2026-08-17.md](./docs/HANDOFF-2026-08-17.md)** — [#10](https://github.com/ainoamn/WAZEN/pull/10) ثم [#11](https://github.com/ainoamn/WAZEN/pull/11) ثم [#12](https://github.com/ainoamn/WAZEN/pull/12) ثم [#16](https://github.com/ainoamn/WAZEN/pull/16)

- تخفيض الباقة إلى البداية لم يعد يخفي بنود الشريط الجانبي. البنود المقفلة تحمل شارة ترقية وتفتح ملاحظة لترقية الباقة.
- ترقيم أحدث العمليات: 5 / 10 / 50 / 100 (حد 100). تصفية وتصفير ظاهرة على شريط المحفظة الشخصية.
- حدود الاشتراك ([#16](https://github.com/ainoamn/WAZEN/pull/16)، `SCHEMA_VERSION = 7`): محافظ، أعضاء، مستخدمون، معاملات إجمالية/يومية/شهرية، سجلات، مطبوعات شهرية. المشاركة: بريد، واتساب، تنزيل إيصالات. الواجهة ترفض التجاوز: `PLAN_TRANSACTION_LIMIT` / `PLAN_DAILY_TRANSACTION_LIMIT` / `PLAN_MONTHLY_TRANSACTION_LIMIT` / `PLAN_RECORD_LIMIT` / `PLAN_USER_LIMIT` / `PLAN_PRINT_LIMIT` / `PLAN_FEATURE_REQUIRED`. تنبيه عند 80%. زر إدارة المنصة للطاقم فقط. مدمج في `main`.
- `/admin/plans`: جدول مقارنة — عمود ثابت «العناوين»، رأس كل باقة، ثم صفوف التحكم (هوية، سعر، حدود بما فيها اليومي/الشهري/المطبوعات، خصائص المشاركة، بوابات). الحفظ لكل عمود أو لكل التغييرات.
- بقية الإدارة ([#13](https://github.com/ainoamn/WAZEN/pull/13)): نظرة عامة، المستخدمون، المستأجرون، الفريق، البوابات، المدفوعات، التقارير، والإيصالات بنفس لغة الجدول (أعمدة + صف عناوين + تحكم). مدمج في `main`.
- تعريب لوحة الإدارة ([#14](https://github.com/ainoamn/WAZEN/pull/14)): الأدوار، التدقيق، طرق الدفع، التسوية، الدول، التقويم، أزرار الخطأ، وعناوين الصفحات بالعربية والإنجليزية. مدمج في `main`.
- استكمال التعريب ([#15](https://github.com/ainoamn/WAZEN/pull/15)): نطاق البوابات، رسائل الخطأ، تواريخ الإدارة، رؤوس CSV، وطرق دفع المستندات عبر `lib/admin-labels.ts`. مدمج في `main`.
- زر **تسجيل الخروج** في رأس `/admin` وأسفل الشريط الجانبي (كل صفحات الإدارة). علامة **و** في الرأس تفتح قائمة الحساب (الرئيسية، أمان الحساب، تسجيل الخروج).
- جلسة المتصفح تُحذف عند إغلاق المتصفح، وتُلغى بعد **10 دقائق** سكون. **جلسة واحدة لكل متصفح** (`browser_id` + cookie `wazen_browser`): تسجيل دخول جديد يلغي كل جلسات نفس الجهاز؛ التبويبات تتزامن؛ `/login` و`/register` يُعاد توجيههما من الخادم عند وجود جلسة؛ لا يمكن تسجيل حساب ثانٍ دون خروج (`SESSION_ALREADY_ACTIVE`). مدمج: [#17](https://github.com/ainoamn/WAZEN/pull/17) · [#30](https://github.com/ainoamn/WAZEN/pull/30) · [#31](https://github.com/ainoamn/WAZEN/pull/31) · [#32](https://github.com/ainoamn/WAZEN/pull/32).
- تسجيل الدخول عبر **جوجل** من `/login` و`/register` بنفس أسلوب حسابي: Google Identity Services (`id_token`) يُتحقق منه في الخادم **بدون Client Secret**. المعرّف العام لعميل One BHD احتياطي في الكود. مسار الإرجاع + PKCE اختياري إن وُجد السر. مدمج: [#33](https://github.com/ainoamn/WAZEN/pull/33)–[#41](https://github.com/ainoamn/WAZEN/pull/41).
- `/admin` لا يعرض شريط الإدارة إلا لـ `super_admin` / `admin`. الزائر بلا جلسة يُحوَّل لتسجيل الدخول فوراً؛ المستخدم العادي يرى «لا تملك صلاحية» بدون قوائم الإدارة. زر **إدارة المنصة** في لوحة المستخدم يظهر لهذين الدورين فقط.
- فرض الباقة: باقة البداية لا تفتح إلا المحفظة الشخصية. المحافظ والبيانات غير المشمولة في `/admin/plans` لا تُحمَّل. مزامنة مباشرة خفيفة كل 12 ثانية؛ نشر Git على `main` يحدّث Vercel ويعيد تحميل العميل عند تغيّر `buildId`. الترقية بعد الدفع فوراً، والتنزيل من اليوم التالي لانتهاء الفترة. مدمج: [#18](https://github.com/ainoamn/WAZEN/pull/18) · [#19](https://github.com/ainoamn/WAZEN/pull/19).
- احتفاظ بعد التخفيض/الانتهاء (`SCHEMA_VERSION = 9`): تنبيه للمستخدم بـ **15 يوماً** ثم حذف من حسابه (بدون ذكر الاسترجاع). أرشيف إداري **60 يوماً** للاسترجاع بمقابل من ملف المستخدم. مهمة الصيانة تنفّذ الأرشفة والتصفية.
- صفحة الفوترة: إصلاح تجاوز الجداول/الحصص لحدود الصفحة؛ رأس حساب العميل يفتح `/pricing` و`/billing` وليس `/admin/plans`.
- طباعة الإيصالات والتقارير: خطوط أكبر وأوضح على الورق (`lib/print-document.ts`).
- `/home` ولوحة التحكم: إصلاح تحميل البيانات بعد تحديثات الاحتفاظ/الباقة (لا تُسقط الصفحة إذا فشل استعلام فرعي)، ومهلة السكون **10 دقائق**. مدمج: [#24](https://github.com/ainoamn/WAZEN/pull/24).
- الزائر غير المسجّل: زر **تسجيل الدخول** يفتح `/login` مباشرة (لا `/home`). `/home` و`/dashboard` يُحوَّلان من الخادم عند غياب الجلسة، و401 لا يعرض «تعذر تحميل البيانات» قبل التحويل. مدمج: [#47](https://github.com/ainoamn/WAZEN/pull/47).
- التنقّل داخل الحساب (`/home`، `/dashboard`، `/billing`، `/documents`، `/admin`) يستخدم `Link` وذاكرة صفحة؛ لا شاشة شعار كاملة عند كل نقرة داخل الحساب. مدمج: [#48](https://github.com/ainoamn/WAZEN/pull/48).
- التحميل الأول لـ `/home` و`/dashboard` و`/admin` يعيد شعار وازن والقلب (بدل نص «جاري التحميل» فقط). طلبات الفوترة والمستندات والتسعير لا تبدأ مع `/api/dashboard` حتى لا تتوقف الصفحة. مهلة 25 ث للوحة و20 ث للتحقق من الإدارة ثم زر إعادة المحاولة. فرع `cursor/fix-home-admin-hang-b4f9`.
- صفحة **الإعدادات** في لوحة المستخدم تتضمن نموذج تغيير كلمة المرور (الحالية، الجديدة، التأكيد). `/account/security` يبقى لـ TOTP ومفاتيح API. [#50](https://github.com/ainoamn/WAZEN/pull/50).
- بناء Vercel: صفحات العميل لا تستورد وحدة القاعدة `lib/plan-retention` حتى لا تُضمَّن `node:sqlite` في المتصفح. مدمج: [#25](https://github.com/ainoamn/WAZEN/pull/25).
- لوحة الإدارة: تنبيهات (مدفوعات، باقات، حسابات، مراسلات)، تواريخ الباقة في قائمة المستخدمين، سجل IP في ملف المستخدم. حظر العنوان **ساعتان فقط** ويمكن رفعه فوراً من الإدارة. `SCHEMA_VERSION = 10`. مدمج: [#26](https://github.com/ainoamn/WAZEN/pull/26).
- زر الطباعة في المحفظة الشخصية: لا يعيد تحميل اللوحة كاملة بعد تسجيل الحصة، حتى لا يظهر `INTERNAL_ERROR`.
- كشف الطباعة: A4 عرضي للجداول العريضة، تواريخ وأرقام أصغر، حتى لا يخرج السحب أو البيان عن الصفحة.

---

## 3. ما هي المنصة وماذا تقدّم

### 3.1 الفكرة المركزية

> كل مال في حسابه الصحيح: صندوق مشترك قابل للصرف، وفائض شخصي محمي لا يدخل مصروفات المجموعة إلا بقرار صاحبه.

### 3.2 الخدمات / الوحدات الوظيفية

| الخدمة | الوصف | مستوى التنفيذ |
|--------|--------|----------------|
| محفظة شخصية | دخل، مصروف، رصيد | مدعوم (seed + إضافة عملية/محفظة) |
| ميزانية منزل | مصروفات أسرية | seed + عرض واجهة |
| رحلات وتسويات | مساهمات، تعويضات، هدف | جزئي (منطق allocation / personal_reserve) |
| جمعيات | أقساط وأدوار | عرض UI + seed؛ قرعة حقيقية ناقصة |
| الأعضاء والأدوار | مالك، أمين صندوق، عضو… | أدوار في schema؛ صلاحيات API جزئية |
| دعوات الأعضاء | دعوة بالبريد + دور | `inviteMember` API |
| باقات الاشتراك | بداية / عائلة / احتراف / أعمال | `plans` + UI pricing |
| كوبونات | تحقق + تطبيق خصم | `validateCoupon` / `selectPlan` |
| فواتير ومدفوعات | إنشاء فاتورة + سجل | محاكاة (بدون بوابة دفع) |
| مستندات | إيصالات / صرف / تسليم / كشوف | إنشاء + ترقيم تسلسلي |
| إدارة المنصة | عملاء، أدوار، مدفوعات، تقارير | API admin + UI |
| سجل تدقيق | `audit_logs` | يُكتب على بعض الإجراءات |
| ثنائية اللغة | ar / en | واجهات commerce + dashboard |
| عملات | SAR افتراضياً (+ عرض متعدد لاحقاً) | حقل currency في schema |

### 3.3 ما لا تقدّمه المنصة حالياً (بوضوح)

- تحويل بنكي أو دفع عبر Thawani / OmanNet / Stripe حقيقي  
- تطبيق جوال أصلي  
- محاسبة محاسبية رسمية أو ربط بنكي (Open Banking)  
- تشفير حقول مالية من طرف لطرف  
- مصادقة إنتاج (بريد/هاتف/OAuth كامل مستقل عن OpenAI Sites)

---

## 4. كيف يعمل البرنامج

### 4.1 نظرة معمارية

```
المتصفح (React Client Components)
        │  fetch JSON
        ▼
Next.js App Router  ── app/* pages
        │
        ├── /api/dashboard  ── المحافظ والعمليات
        └── /api/platform   ── الباقات، الفوترة، المستندات، الإدارة
                │
                ▼
        db/runtime.ts
           ├── Cloudflare D1  (عند vinext / Sites + binding DB)
           └── Node SQLite    (عند Vercel أو WAZEN_USE_NODE_SQLITE=1)
                │
                ▼
        جداول schema (users, spaces, transactions, …)
```

### 4.2 تدفق المستخدم النموذجي

1. يفتح `/` (تسويق) ثم `/dashboard`.  
2. الواجهة تستدعي `GET /api/dashboard`.  
3. السيرفر يحدد المستخدم (`getRequestUser`):  
   - headers OpenAI SIWC إن وُجدت، أو  
   - مستخدم demo على localhost / Vercel.  
4. `ensureSchema` ينشئ الجداول إن لم تكن.  
5. `seedUser` يزرع بيانات أولية (محافظ، أعضاء، عمليات) لأول مرة.  
6. تُعاد البيانات كـ JSON وتُعرض في `wazen-dashboard.tsx`.  
7. إضافة محفظة/عملية → `POST /api/dashboard` مع `action`.

### 4.3 تدفق الاشتراك / الإدارة

1. `/pricing` ← `GET /api/platform?view=pricing`  
2. اختيار باقة / كوبون ← `POST` actions: `validateCoupon`, `selectPlan`  
3. `/billing` ← فواتير ومدفوعات المستخدم  
4. `/documents` ← إنشاء مستندات مرقمة  
5. `/admin` ← يتطلب دور admin في `platform_roles` (أول مستخدم غالباً `super_admin`)

### 4.4 بيئتا التشغيل

| البيئة | البناء | قاعدة البيانات | المصادقة |
|--------|--------|----------------|----------|
| Cloudflare Sites / vinext | `vinext build` | D1 (`DB`) | headers SIWC اختيارية |
| Vercel / Next | `next build` | SQLite في `/tmp` أو `.data/` | demo user |
| تطوير vinext محلي | `vinext dev` | D1 محلي عبر plugin | demo / headers |

### 4.5 فصل الأرصدة (منطق منتج مهم)

عند إضافة عملية (`addTransaction`):

- أنواع موجبة للرصيد المشترك: `income`, `contribution` (ما لم تكن `personal_reserve`)  
- `allocation === "personal_reserve"` → لا يزيد `spaces.balance_minor`؛ يزيد `members.extra_minor`  
- غير ذلك للخارج: ينقص رصيد الصندوق  

هذا يطابق وعد المنتج: **لا خلط بين الفائض الشخصي والصندوق المشترك**.

---

## 5. الصفحات والواجهات

| المسار | المكوّن الرئيسي | الوظيفة |
|--------|------------------|---------|
| `/` | `app/page.tsx` | صفحة تسويق commercial |
| `/dashboard` | `app/wazen-dashboard.tsx` | لوحة المستخدم الكاملة |
| `/pricing` | `pricing-client.tsx` | الباقات والكوبونات |
| `/billing` | `billing-client.tsx` | اشتراك وفواتير المستخدم |
| `/documents` | `documents-client.tsx` | إيصالات وكشوفات |
| `/admin` | `admin-client.tsx` | نظرة إدارة |
| `/admin/users` | admin users | العملاء |
| `/admin/payments` | payments | المدفوعات |
| `/admin/reports` | reports | الإيرادات |

مكوّنات مشتركة: `app/commercial-kit.tsx` (Brand, Header, AdminShell, locale).  
تنسيق: `globals.css` + `commercial.css` + Tailwind 4 + أيقونات `lucide-react`.

على الهاتف (عرض ≤ 900px): اللوحات المنبثقة `position: fixed` داخل الإطار مع تمرير داخلي؛ لا تعتمد على `overflow-x: clip` على `html` و`body` معاً. التفاصيل في التسليم §2.3.

### اتجاه الواجهة

- افتراضي: عربي RTL  
- تبديل EN/AR مع حفظ في `localStorage` (`wazen-locale`)

---

## 6. واجهات API

### 6.1 `/api/dashboard`

| Method | الوظيفة |
|--------|---------|
| `GET` | مستخدم + spaces + members + transactions + contribution_plans |
| `POST` `action=addWallet` | إنشاء مساحة/محفظة |
| `POST` `action=addTransaction` | تسجيل عملية وتحديث الأرصدة |

### 6.2 `/api/platform`

| view / action | الوظيفة |
|---------------|---------|
| `GET ?view=pricing` | الباقات العامة (بدون auth) |
| `GET ?view=documents` | مستندات المستخدم |
| `GET ?view=billing` | اشتراك + فواتير + مدفوعات |
| `GET ?view=admin` | بيانات الإدارة (يتطلب admin) |
| `POST validateCoupon` | التحقق من كوبون |
| `POST selectPlan` | اختيار باقة + فاتورة + ضريبة 15% |
| `POST createDocument` | مستند + رقم تسلسلي `WZN-…` |
| `POST inviteMember` | دعوة عضو لمحفظة |
| `POST setUserStatus` | admin: حالة عميل |
| `POST setRole` | admin: دور منصة |
| `POST setPaymentStatus` | admin: حالة دفع |
| `POST createCoupon` | admin: كوبون جديد |

---

## 7. قاعدة البيانات

### 7.1 الجداول (Drizzle / `db/schema.ts`)

تقريباً **17 جدولاً**:

| جدول | الغرض |
|------|--------|
| `users` | المستخدمون |
| `spaces` | المحافظ/المساحات |
| `members` | أعضاء المساحة |
| `contribution_plans` | خطط المساهمة الدورية |
| `transactions` | العمليات المالية |
| `platform_roles` | أدوار إدارة المنصة |
| `customer_profiles` | ملف العميل التجاري |
| `plans` | باقات الاشتراك |
| `subscriptions` | اشتراكات المستخدمين |
| `invoices` | الفواتير |
| `payments` | المدفوعات |
| `coupons` | الكوبونات |
| `documents` | المستندات الرسمية |
| `document_sequences` | تسلسل الترقيم |
| `invites` | دعوات الأعضاء |
| `audit_logs` | سجل التدقيق |

### 7.2 تمثيل المبالغ

تُخزَّن المبالغ كـ **وحدات صغرى** (`*_minor`) — مثال: هللة/فلس/سنت — لتجنب أخطاء الفاصلة العشرية.

### 7.3 التهيئة

- `db/runtime.ts` → `ensureSchema()` ينشئ الجداول بـ `CREATE TABLE IF NOT EXISTS`  
- migrations في `drizzle/0000_*.sql` و `0001_*.sql`  
- `.openai/hosting.json`: `"d1": "DB"` مفعّل لمسار Cloudflare  

### 7.4 Vercel / التخزين

- ملف SQLite: `/tmp/wazen.sqlite` على Vercel  
- محلياً مع Next: `.data/wazen.sqlite`  
- **تحذير:** على Vercel serverless التخزين في `/tmp` **غير دائم** بين المثيلات والإعادة التشغيل — مناسب للتجربة وليس لإنتاج.

---

## 8. التقنيات المستخدمة

### 8.1 الواجهة والتطبيق

| التقنية | الإصدار (تقريبي) | الدور |
|---------|------------------|--------|
| React | 19.2.6 | UI |
| Next.js | 16.2.6 | App Router / نشر Vercel |
| TypeScript | 5.9.3 | لغة |
| Tailwind CSS | 4.2.1 | تنسيق |
| lucide-react | ^0.544 | أيقونات |
| vinext | 1.0.0-beta.2 | تشغيل Next على Cloudflare |
| Vite | 8.0.13 | bundler مع vinext |
| Wrangler | 4.92.0 | أدوات Cloudflare |

### 8.2 البيانات والخلفية

| التقنية | الدور |
|---------|--------|
| Cloudflare D1 | SQLite على الحافة (المسار الأساسي Sites) |
| Node `node:sqlite` | بديل D1 على Vercel (تجريبي) |
| Drizzle ORM | تعريف schema + (مسار drizzle اختياري) |
| SQL خام في API routes | أغلب منطق الأعمال عبر D1 prepare/batch |

### 8.3 أدوات الجودة والنشر

| الأداة | الدور |
|--------|--------|
| ESLint + typescript-eslint | lint |
| Node test (`tests/rendered-html.test.mjs`) | اختبار worker HTML (vinext) |
| GitHub | مصدر الشفرة |
| Vercel | استضافة/نشر main |
| OpenAI Sites bindings | ملفات `.openai/hosting.json` |

### 8.4 Node

```json
"engines": { "node": ">=22.13.0" }
```

---

## 9. الإيجابيات

1. **فكرة منتج واضحة** ومميّزة (فصل صندوق مشترك / فائض شخصي).  
2. **MVP حقيقي** وليس مجرد mock: قاعدة بيانات + APIs + واجهات متصلة.  
3. **نموذج أعمال SaaS** (باقات، كوبونات، فواتير، إدارة).  
4. **ثنائية لغة ar/en** مبكرة.  
5. **مخطط بيانات ثري** يناسب التوسع (17 جدولاً).  
6. **مبالغ minor units** ممارسة مالية صحيحة.  
7. **سجل تدقيق** لبعض الإجراءات الحساسة.  
8. **مسارات نشر مزدوجة** (Cloudflare vinext + Vercel Next).  
9. **أيقونات وواجهة commercial** أحدث من أرشيف رِفد.  
10. **أرشفة رِفد** تحافظ على أفكار الشاشات بدون تلويث `main`.  
11. **عزل ملكية المحفظة** جزئياً (`owner_user_id` عند الكتابة).  
12. **ترقيم مستندات** تسلسلي (`document_sequences`).

---

## 10. السلبيات

1. **ليس منتج إنتاج** — demo auth + تخزين مؤقت على Vercel.  
2. **Auth غير إنتاجي** (headers OpenAI أو مستخدم demo ثابت).  
3. **أول مستخدم = super_admin** — خطر في أي بيئة مشتركة.  
4. **لا بوابة دفع حقيقية**.  
5. **SQL خام طويل** داخل route handlers (صعب الاختبار والصيانة).  
6. **صلاحيات الأعضاء داخل المحفظة** غير مفروضة بالكامل على كل endpoint.  
7. **القرعة، التصويت، التسوية الذكية، PDF رسمي** — واجهة/وصف أكثر من تنفيذ مكتمل.  
8. **ازدواج وقت تشغيل** (D1 vs SQLite) يزيد تعقيد الصيانة.  
9. **اختبارات آلية ضعيفة** (اختبار HTML واحد تقريباً لمسار vinext).  
10. **`node:sqlite` تجريبي** مع تحذيرات على Node 22.  
11. **لا نظام إشعارات** (بريد/SMS).  
12. **نسخ احتياطي / ترحيل بيانات إنتاج** غير مكتمل على Vercel.  
13. **توثيق تشغيلي بالإنجليزية في README الأصلي** كان starter؛ يُستكمل بهذا الملف.  
14. **عروض الأمان في التسويق** أقوى من التنفيذ الفعلي — خطر توقعات المستخدم.

---

## 11. الحماية والتشفير

### 11.1 طبقات الحماية الفعلية (تقدير واقعي)

| # | الطبقة | الحالة | ملاحظة |
|---|--------|--------|--------|
| 1 | HTTPS عند الاستضافة | ✅ على Vercel/Cloudflare | من المنصة لا من التطبيق |
| 2 | مصادقة هوية مستخدم | ⚠️ ضعيفة | demo أو headers خارجية فقط |
| 3 | تفويض (Authorization) | ⚠️ جزئي | admin assert؛ باقي endpoints محدودة |
| 4 | عزل بيانات المستأجرين | ⚠️ جزئي | فلترة `owner_user_id` في أماكن |
| 5 | التحقق من المدخلات | ⚠️ أساسي | تحقق نوع/مبلغ/حقول مطلوبة |
| 6 | CSRF / Rate limit | ❌ | غير منفّذ |
| 7 | WAF / Bot protection | ⚠️ | يعتمد على المضيف فقط |
| 8 | Audit logging | ⚠️ | لبعض الأفعال admin/منصة |
| 9 | إدارة أسرار (.env) | ✅ جزئي | `.env*` في gitignore |
| 10 | تشفير بيانات في الحقول | ❌ | لا يوجد field-level encryption |
| 11 | hashing كلمات مرور | ❌ | لا كلمات مرور أصلاً |
| 12 | JWT / sessions | ❌ | لا sessions تطبيقية كلاسيكية |

**عدد طبقات الحماية الفعّالة فعلياً داخل التطبيق: ~1–2**  
(HTTPS + بعض فحوصات الملكية/الأدوار).  

**طبقات التشفير داخل التطبيق: 0**  
(لا تشفير تطبيق لـ PII/أرصدة؛ الاعتماد على TLS النقل فقط عند الاستضافة).

### 11.2 ما ينقص من حماية وتشفير

1. نظام Auth إنتاج (Auth.js / Clerk / Cognito / OTP هاتف).  
2. جلسات آمنة + HttpOnly cookies + CSRF.  
3. RBAC كامل على كل API (عضو يرى حسابه فقط).  
4. Rate limiting على login وAPI المالية.  
5. تشفير PII حساس عند التخزين إن لزم (هاتف، مستندات).  
6. HSTS, CSP, permissions-policy headers.  
7. إدارة أسرار الدفع (vault) + webhooks موقّعة.  
8. 2FA للإدارة.  
9. تدقيق لا يُحذف ومراقبة anomalies.  
10. نسخ احتياطي مشفّر واستعادة مجرّبة.  
11. سياسة كلمات مرور إن وُجدت.  
12. فصل بيئة admin على نطاق/صلاحيات أضيق.

### 11.3 إعلانات الأمان مقابل الواقع

نصوص مثل «بيانات مشفرة / سجل تدقيق / صلاحيات دقيقة» في الواجهة التسويقية:

- **سجل تدقيق:** موجود جزئياً.  
- **صلاحيات:** جزئية على مستوى المنصة.  
- **تشفير بيانات التطبيق:** غير منفّذ كميزة منتج.

---

## 12. الثغرات والمشاكل الفنية

> ليست قائمة pentest رسمية؛ هي ملاحظات من مراجعة الشفرة وبنية الـ MVP.

### 12.1 أمنية

| الأولوية | المشكلة | الأثر |
|----------|---------|--------|
| حرج | مستخدم demo ثابت على Vercel | أي زائر = نفس الهوية + بيانات قابلة للتداخل في بعض الحالات |
| حرج | أول مسجّل = super_admin | تصعيد امتيازات |
| عالٍ | عدم وجود auth حقيقي على `/admin` في بعض المسارات UI | واجهة ظاهرة؛ الاعتماد على API 403 فقط |
| عالٍ | لا rate limit | تخمين/إغراق API |
| متوسط | SQL مبني من سلاسل ثابتة + bind | جيد عموماً؛ خطر مستقبلي إن دُمجت مدخلات خام |
| متوسط | invite token يُعاد في JSON | يجب أن يُرسل قناة آمنة فقط |
| متوسط | كوبونات بلا قفل سباق قوي | over-use محتمل |
| منخفض | رسائل خطأ قد تُسرّب تفاصيل داخلية | معلومات لمهاجم |

### 12.2 فنية / موثوقية

| المشكلة | التفاصيل |
|---------|----------|
| تخزين Vercel غير دائم | `/tmp` يضيع |
| seed بيانات وهمية | قد يختلط بالبيانات الحقيقية لاحقاً إن لم يُعزل |
| D1 adapter Node غير مكتمل 100% | batch/run قد يختلف سلوكه عن D1 الحقيقي في حدود الحالات |
| قلة الاختبارات | انحدار سهل عند التعديل |
| routes عملاقة | `platform/route.ts` مئات الأسطر — صعوبة مراجعة |
| vinext beta | اعتماد على إصدار غير نهائي |
| تحذير engines Node | `>=22` قد يتحرك major تلقائياً على المضيف |

### 12.3 منتج / امتثال

- لا سياسة خصوصية/شروط استخدام منفّذة داخل التطبيق.  
- لا موافقة صريحة على معالجة بيانات مالية.  
- أسماء/إيميلات demo قد تُفهم كبيانات حقيقية.  
- الإطلاق في أسواق خليجية قد يتطلب اعتبارات تنظيمية (مدفوعات/بيانات).

---

## 13. ما ينقص للإطلاق والتجربة

### 13.1 لتجربة داخلية / Demo جيد (حد أدنى)

| المتطلب | الحالة |
|---------|--------|
| نشر مستقر (Vercel أو Cloudflare) | ⚠️ قيد الاعتماد بعد إصلاح build |
| واجهة مستخدم قابلة للتجول | ✅ |
| بيانات seed مفهومة | ✅ |
| تعليمات تجريبية قصيرة | ⚠️ تحتاج صفحة/دليل demo |
| عزل بيانات demo لكل زائر | ❌ (هوية demo مشتركة على Vercel) |
| Reset demo data | ❌ |

**للتجربة الأفضل قبل عرض للمستثمر:**  
هوية فريدة لكل جلسة demo + تخزين دائم (Turso/Neon/D1 حقيقي) + زر «إعادة ضبط البيانات».

### 13.2 لإطلاق عام محدود (Beta)

1. Auth حقيقي + تحقق بريد/هاتف.  
2. قاعدة بيانات دائمة ومدارة.  
3. نسخ احتياطي يومي.  
4. سياسات خصوصية وشروط.  
5. مراقبة أخطاء (Sentry) وسجلات.  
6. إخفاء/تأمين `/admin` بصرامة.  
7. دفع تجريبي sandbox أو فوترة يدوية واضحة.  
8. اختبارات أساسية للـ API المالي.  
9. بيئة staging منفصلة عن production.  
10. خطة دعم/بلاغ أعطال.

### 13.3 لإطلاق إنتاج مالي جدّي

1. بوابة دفع مرخّصة (حسب الدولة: Thawani، HyperPay، Stripe…).  
2. امتثال (PCI عبر المزود؛ حماية بيانات؛ حذف حساب).  
3. مراجعات أمنية ودوريات.  
4. ledger غير قابل للتلاعب (append-only للحركات).  
5. صلاحيات موافقات متعددة للمبالغ الكبيرة.  
6. تقارير ضريبية/محاسبية.  
7. SLA وuptime.  
8. فريق تشغيل on-call.

---

## 14. اقتراحات التطوير

### 14.1 البنية والكود (أولوية عالية)

1. فصل **Services / Repositories** عن route handlers.  
2. استخدام Drizzle queries بدل SQL المتفرق حيث أمكن.  
3. طبقة `AuthContext` موحّدة.  
4. اختبارات: unit للأرصدة + integration للـ API.  
5. اختيار **مصدر حقيقة واحد** للإنتاج:  
   - إما Cloudflare D1 بالكامل،  
   - أو Postgres (Neon) + Vercel،  
   - وتبسيط المسار الآخر.  
6. Migrations رسمية بدل الاعتماد فقط على `CREATE IF NOT EXISTS`.

### 14.2 المنتج المالي

1. تسوية ذكية (min transfers) حقيقية.  
2. موافقات متعددة المستويات.  
3. ميزانيات وحدود تنبيه.  
4. قرعة موثّقة ببذرة hash + سجل.  
5. تصدير PDF/Excel حقيقي موقّع/مرقّم.  
6. عملات حيّة (معدلات API) بدل ثابتة.  
7. إشعارات استحقاق المساهمة.

### 14.3 الصفحة الرئيسية والتسويق

1. CTA أوضح لمسار demo بدون خلط مع admin.  
2. شفافية: «نسخة تجريبية» في الشريط.  
3. دراسات حالة/قصص استخدام (عائلة، رحلة، جمعية).  
4. صفحة أمان واقعية (ماذا نفعل فعلياً).  
5. SEO/بيانات منظمة + مدونة لاحقاً.

### 14.4 لوحة المستخدم `/dashboard`

1. تقسيم الملف الضخم `wazen-dashboard.tsx` إلى وحدات.  
2. رسوم بيانية تفاعلية حقيقية من البيانات.  
3. فلترة وبحث عمليات على السيرفر.  
4. حالات تحميل/أخطاء أغنى.  
5. وضع «فقط قراءة» للمدقق.  
6. استيراد/تصدير CSV.  
7. إشعارات داخل التطبيق.

### 14.5 الإدارة `/admin`

1. لا تعتمد «أول مستخدم super_admin» — bootstrap بسريّات بيئة.  
2. فصل أدوار finance / support فعلياً في UI.  
3. رسوم MRR حقيقية من الجداول.  
4. سجل تدقيق قابل للبحث والتصفية.  
5. إيقاف حساب + تجميد محافظ.

### 14.6 التسعير والفوترة

1. ربط webhook دفع.  
2. فواتير PDF ضريبية.  
3. حدود الباقة تُفرض في API (`wallet_limit`, `member_limit`).  
4. تجربة free trial واضحة المدة.

### 14.7 الواجهة والتصميم

1. توحيد نظام تصميم (ألوان، مسافات، مكوّنات).  
2. تحسين الموبايل (drawer للإدارة والداشبورد).  
3. إمكانية وصول (a11y): تباين، focus، aria.  
4. وضع طباعة أفضل للمستندات.  
5. الاستفادة انتقائياً من شاشات أرشيف **رِفد** (كثافة السيناريوهات، هوية عُمانية إن رُغبت) دون الرجوع لـ mock.

### 14.8 الاستفادة من أرشيف رِفد

من `archive/rifd` يمكن استعادة:

- سيناريوهات UI أغنى (جمعية، منزل، سفر).  
- هوية «عُمان / OMR» إن كان السوق المستهدف محلياً.  
- قوالب إيصالات بصرية.  

**لا** تُعاد المنصة إلى mock بدون API.

---

## 15. هيكل المشروع والمسارات المحلية

### 15.1 على القرص

```
C:\dev\WAZEN                 → المنصة الرئيسية (فرع main)
C:\dev\WAZEN-archive-rifd    → أرشيف رِفد (فرع archive/rifd ، git worktree)
```

### 15.2 أهم مجلدات المستودع (`main`)

```
app/                  صفحات الواجهة + API routes
  api/dashboard/      منطق المحافظ والعمليات
  api/platform/       المنصة التجارية والمستندات
  wazen-dashboard.tsx لوحة المستخدم
  commercial-kit.tsx  مكوّنات مشتركة
db/
  schema.ts           تعريف الجداول
  runtime.ts          ensureSchema + getRequestUser + getRawDb
  node-sqlite-d1.ts   محوّل SQLite لـ Vercel
  index.ts            drizzle getDb
drizzle/              SQL migrations
worker/               مدخل Cloudflare Worker (vinext)
lib/                  stub cloudflare:workers
scripts/build.mjs     بناء مزدوج Vercel/vinext
public/               أصول + OG images
tests/                اختبارات
.openai/hosting.json  D1 binding
```

### 15.3 Git worktrees

```bash
git -C C:\dev\WAZEN worktree list
# C:/dev/WAZEN              [main]
# C:/dev/WAZEN-archive-rifd [archive/rifd]
```

---

## 16. أوامر التشغيل والبناء

### المتطلبات

- Node.js **≥ 22.13.0**

### أوامر شائعة

```bash
cd C:\dev\WAZEN
npm install

# تطوير vinext (Cloudflare-oriented)
npm run dev

# بناء تلقائي dual
npm run build

# بناء صريح
npm run build:next     # Next.js (Vercel)
npm run build:vinext   # Cloudflare artifact

npm run start          # vinext start
npm run start:next     # next start
npm run db:generate
npm test
npm run lint
```

### متغيرات بيئة مفيدة

| المتغير | المعنى |
|---------|--------|
| `VERCEL=1` | يضبطه Vercel؛ build بـ next + demo auth |
| `WAZEN_USE_NODE_SQLITE=1` | إجبار Node SQLite |
| `WAZEN_SQLITE_PATH` | مسار ملف SQLite مخصص |
| `WAZEN_DEMO_AUTH=1` | تفعيل demo user خارج localhost/Vercel |
| `SITES_*` | (أرشيف رِفد) مهلات البناء القديمة |

---

## 17. خارطة الطريق المقترحة

### المرحلة 0 — استقرار Demo (1–2 أسابيع)

- [x] اعتماد وازن على `main`  
- [x] إصلاح build Vercel  
- [ ] هوية demo فريدة لكل زائر  
- [ ] DB دائمة (Turso/Neon/D1)  
- [ ] بانر «نسخة تجريبية»  
- [ ] دليل تجربة من 5 خطوات  

### المرحلة 1 — Beta مغلق

- [ ] Auth حقيقي  
- [ ] فرض حدود الباقات  
- [ ] تأمين admin  
- [ ] اختبارات API  
- [ ] مراقبة أخطاء  

### المرحلة 2 — Beta مفتوح

- [ ] بوابة دفع sandbox  
- [ ] مستندات PDF  
- [ ] إشعارات بريد  
- [ ] تحسين الموبايل  

### المرحلة 3 — إطلاق 1.0

- [ ] دفع إنتاج  
- [ ] سياسة خصوصية/امتثال  
- [ ] Ledger صارم + موافقات  
- [ ] نسخ احتياطي واستعادة  
- [ ] مراجعة أمنية  

---

## 18. ملحق: العلاقة برِفد

| | رِفد (أرشيف) | وازن (رئيسي) |
|--|--------------|---------------|
| الفكرة | إدارة مال شخصي/مشترك | نفسها + طبقة SaaS أوضح |
| التنفيذ | UI mock بالكامل | APIs + DB + UI |
| الهوية | عُمانية RIFD / OMR | عالمية عربية WAZEN / SAR |
| الفرع | `archive/rifd` | `main` |
| الاستخدام الآن | مرجع أفكار وتصميم | التطوير والنشر |

**القرار المعتمد:** التطوير المستمر على **وازن**؛ رِفد مرجعية محفوظة فقط.

---

## خاتمة

**وازن** منصة واعدة بفكرة مالية واضحة ونواة تقنية تتجاوز مرحلة الـ mock: لديها جداول، APIs، واجهات، ومسار نشر.  
في المقابل: الحماية ضعيفة، لا تشفير تطبيقي، المصادقة تجريبية، والدفع غير موصول، وتخزين Vercel غير مناسب لإنتاج مالي.

**الجملة العملية:**  
جاهز لـ **عرض تجريبي مضبوط** بعد تثبيت هوية وDB دائمة؛  
**غير جاهز** لإطلاق عام أو حمل أموال حقيقية قبل استكمال طبقات المصادقة، التخزين الدائم، الدفع، والاختبارات الأمنية.

---

*نهاية ملف التوثيق الكامل — وازن | WAZEN v0.1.0*  
*للاستفسارات التشغيلية راجع أيضاً `README.md` في جذر المشروع.*
